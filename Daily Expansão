<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sorteador da Daily | Expansão · Frenet</title>

  <style>
    :root {
      --navy: #100A66;
      --purple: #7B3FF2;
      --blue: #4F6FFF;
      --teal: #32C3AD;
      --bg: #F6F8FC;
      --surface: #FFFFFF;
      --surface-soft: #FAFBFF;
      --line: #E2E7F2;
      --text: #26324A;
      --muted: #667187;
      --muted-2: #8792A6;
      --success: #19A77D;
      --warning: #F4B23E;
      --danger: #D94F70;
      --shadow: 0 16px 44px rgba(18, 24, 59, .08);
      --radius-xl: 24px;
      --radius-lg: 16px;
    }

    * {
      box-sizing: border-box;
    }

    html,
    body {
      width: 100%;
      height: 100%;
      margin: 0;
      overflow: hidden;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system,
        BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
      color: var(--text);
      background:
        radial-gradient(circle at 5% 0%, rgba(123, 63, 242, .10), transparent 32%),
        radial-gradient(circle at 100% 0%, rgba(50, 195, 173, .12), transparent 30%),
        linear-gradient(180deg, #FFFFFF 0%, var(--bg) 100%);
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      background:
        linear-gradient(90deg, rgba(16, 10, 102, .025) 1px, transparent 1px),
        linear-gradient(180deg, rgba(16, 10, 102, .022) 1px, transparent 1px);
      background-size: 56px 56px;
      opacity: .55;
    }

    .app {
      position: relative;
      z-index: 1;
      height: 100vh;
      width: min(1440px, calc(100vw - 28px));
      margin: 0 auto;
      padding: 18px 0;
      display: grid;
      grid-template-rows: auto minmax(0, 1fr);
      gap: 14px;
    }

    .topbar {
      min-height: 76px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: start;
      gap: 18px;
    }

    .eyebrow {
      margin-bottom: 5px;
      color: var(--purple);
      font-size: 12px;
      line-height: 1;
      font-weight: 850;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    h1 {
      margin: 0;
      color: var(--navy);
      font-size: clamp(28px, 3vw, 40px);
      line-height: 1.05;
      letter-spacing: -.045em;
      font-weight: 900;
    }

    .title-row {
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 10px;
    }

    .team-chip {
      display: inline-flex;
      align-items: center;
      min-height: 28px;
      padding: 6px 10px;
      border-radius: 999px;
      color: #147B69;
      background: rgba(50, 195, 173, .11);
      border: 1px solid rgba(50, 195, 173, .20);
      font-size: 11px;
      line-height: 1;
      font-weight: 900;
      letter-spacing: .08em;
      text-transform: uppercase;
      white-space: nowrap;
    }

    .subtitle {
      max-width: 900px;
      margin: 8px 0 0;
      color: var(--muted);
      font-size: clamp(13px, 1.1vw, 15px);
      line-height: 1.4;
    }

    .status-pill {
      display: inline-flex;
      align-items: center;
      gap: 9px;
      padding: 10px 14px;
      border-radius: 999px;
      background: rgba(255,255,255,.9);
      border: 1px solid var(--line);
      color: var(--navy);
      font-size: 13px;
      font-weight: 800;
      white-space: nowrap;
      box-shadow: 0 10px 26px rgba(18,24,59,.07);
    }

    .dot {
      width: 9px;
      height: 9px;
      border-radius: 999px;
      background: var(--warning);
      box-shadow: 0 0 0 5px rgba(244,178,62,.15);
    }

    .layout {
      min-height: 0;
      display: grid;
      grid-template-columns: minmax(0, 1.12fr) minmax(360px, .88fr);
      gap: 14px;
    }

    .panel {
      min-height: 0;
      overflow: hidden;
      background: rgba(255,255,255,.94);
      border: 1px solid rgba(226,231,242,.96);
      border-radius: var(--radius-xl);
      box-shadow: var(--shadow);
    }

    .panel-inner {
      height: 100%;
      min-height: 0;
      padding: 18px;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .main-panel {
      position: relative;
    }

    .main-panel::before {
      content: "";
      position: absolute;
      inset: 0 0 auto 0;
      height: 4px;
      background: linear-gradient(
        90deg,
        var(--purple),
        var(--blue),
        var(--teal)
      );
    }

    .metrics {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 10px;
    }

    .metric {
      min-height: 78px;
      padding: 13px 14px;
      border-radius: var(--radius-lg);
      border: 1px solid var(--line);
      background: linear-gradient(180deg, #FFFFFF 0%, #FBFCFF 100%);
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .metric-label {
      margin-bottom: 7px;
      color: var(--muted);
      font-size: 11px;
      font-weight: 850;
      letter-spacing: .10em;
      text-transform: uppercase;
      white-space: nowrap;
    }

    .metric-value {
      color: var(--navy);
      font-size: clamp(22px, 2vw, 30px);
      line-height: 1;
      font-weight: 900;
      letter-spacing: -.045em;
      font-variant-numeric: tabular-nums;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .progress-card {
      display: grid;
      grid-template-columns: auto minmax(120px, 1fr) auto;
      align-items: center;
      gap: 12px;
      padding: 10px 12px;
      border-radius: var(--radius-lg);
      border: 1px solid var(--line);
      background: var(--surface-soft);
      color: var(--muted);
      font-size: 13px;
      font-weight: 750;
    }

    .progress-wrap {
      height: 8px;
      border-radius: 999px;
      background: #EBEFF8;
      overflow: hidden;
    }

    .progress-bar {
      width: 0%;
      height: 100%;
      border-radius: inherit;
      background: linear-gradient(
        90deg,
        var(--purple),
        var(--blue),
        var(--teal)
      );
      transition: width .22s ease;
    }

    .speaker-card {
      position: relative;
      flex: 1;
      min-height: 220px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      text-align: center;
      padding: 26px;
      border-radius: 24px;
      border: 1px solid var(--line);
      background:
        radial-gradient(circle at 16% 0%, rgba(123,63,242,.10), transparent 36%),
        radial-gradient(circle at 100% 0%, rgba(50,195,173,.13), transparent 31%),
        linear-gradient(180deg, #FFFFFF 0%, #F8FBFF 100%);
    }

    .speaker-card::after {
      content: "";
      position: absolute;
      width: 220px;
      height: 220px;
      right: -96px;
      bottom: -120px;
      border-radius: 999px;
      border: 30px solid rgba(123,63,242,.045);
    }

    .speaker-card.drawing::before {
      content: "";
      position: absolute;
      width: 300px;
      height: 300px;
      border-radius: 999px;
      background:
        conic-gradient(
          from 0deg,
          rgba(123,63,242,0),
          rgba(123,63,242,.22),
          rgba(79,111,255,.34),
          rgba(50,195,173,.26),
          rgba(123,63,242,0)
        );
      opacity: .72;
      animation: rouletteSpin .8s linear infinite;
    }

    .speaker-card.drawing .speaker-name {
      animation: roulettePulse .28s ease-in-out infinite alternate;
    }

    @keyframes rouletteSpin {
      to { transform: rotate(360deg); }
    }

    @keyframes roulettePulse {
      from {
        transform: translateY(0) scale(1);
        opacity: .78;
      }
      to {
        transform: translateY(-1px) scale(1.015);
        opacity: 1;
      }
    }

    .speaker-eyebrow {
      position: relative;
      z-index: 1;
      margin-bottom: 12px;
      color: var(--purple);
      font-size: 12px;
      font-weight: 900;
      letter-spacing: .15em;
      text-transform: uppercase;
    }

    .speaker-name {
      position: relative;
      z-index: 1;
      max-width: 100%;
      margin: 0;
      color: var(--navy);
      font-size: clamp(36px, 4.2vw, 64px);
      line-height: 1.02;
      letter-spacing: -.065em;
      font-weight: 900;
      overflow-wrap: anywhere;
      text-wrap: balance;
    }

    /*
      Cargo e aniversário ficam em uma camada visual separada.
      Assim o nome continua sendo o elemento dominante.
    */
    .speaker-meta {
      position: relative;
      z-index: 1;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      align-items: center;
      gap: 8px;
      margin-top: 18px;
    }

    .speaker-meta.hidden {
      display: none;
    }

    .speaker-badge {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 34px;
      padding: 8px 13px;
      border-radius: 999px;
      font-size: 12px;
      line-height: 1;
      font-weight: 850;
      letter-spacing: .025em;
      border: 1px solid transparent;
      white-space: nowrap;
    }

    .speaker-badge.role {
      color: #5A2ABB;
      background: rgba(123,63,242,.09);
      border-color: rgba(123,63,242,.16);
    }

    .speaker-badge.birthday {
      color: #147B69;
      background: rgba(50,195,173,.11);
      border-color: rgba(50,195,173,.20);
    }

    .speaker-badge.hidden {
      display: none;
    }

    .speaker-email {
      position: relative;
      z-index: 1;
      max-width: 760px;
      margin-top: 11px;
      color: var(--muted-2);
      font-size: 12.5px;
      line-height: 1.4;
      overflow-wrap: anywhere;
    }

    .speaker-timer {
      position: relative;
      z-index: 1;
      margin-top: 16px;
      display: inline-flex;
      align-items: center;
      gap: 7px;
      padding: 5px 9px;
      border-radius: 999px;
      background: rgba(16,10,102,.045);
      color: var(--muted-2);
      font-size: 10px;
      font-weight: 750;
      letter-spacing: .055em;
      text-transform: uppercase;
      font-variant-numeric: tabular-nums;
    }

    .speaker-timer b {
      color: #6E7890;
      font-size: 11px;
      letter-spacing: 0;
    }

    .speaker-timer.warning {
      background: rgba(244,178,62,.12);
      color: #9A6B12;
    }

    .speaker-timer.warning b {
      color: #9A6B12;
    }

    .speaker-timer.over {
      background: rgba(217,79,112,.12);
      color: #A63B57;
    }

    .speaker-timer.over b {
      color: #A63B57;
    }

    .actions {
      display: grid;
      grid-template-columns: 1.18fr 1fr .88fr .96fr;
      gap: 10px;
    }

    button {
      min-height: 46px;
      border: 0;
      border-radius: 13px;
      padding: 12px 14px;
      font: inherit;
      font-size: 14px;
      font-weight: 820;
      cursor: pointer;
      transition:
        transform .08s ease,
        box-shadow .18s ease,
        opacity .18s ease,
        filter .18s ease;
    }

    button:hover:not(:disabled) {
      transform: translateY(-1px);
      filter: brightness(1.02);
      box-shadow: 0 10px 22px rgba(18,24,59,.12);
    }

    button:disabled {
      opacity: .42;
      cursor: not-allowed;
    }

    .primary {
      color: #fff;
      background: linear-gradient(135deg, var(--purple), var(--blue));
    }

    .success {
      color: #fff;
      background: linear-gradient(135deg, var(--teal), #17A97D);
    }

    .warning {
      color: #fff;
      background: linear-gradient(135deg, #F5BA4A, #E99025);
    }

    .danger {
      color: #fff;
      background: linear-gradient(135deg, #EC6C88, var(--danger));
    }

    .secondary {
      color: var(--navy);
      background: #F5F7FC;
      border: 1px solid var(--line);
    }

    .side-inner {
      height: 100%;
      min-height: 0;
      padding: 18px;
      display: grid;
      grid-template-rows: auto minmax(0, 1fr) auto;
      gap: 12px;
    }

    .note-card {
      padding: 13px 14px;
      border-radius: var(--radius-lg);
      border: 1px solid var(--line);
      background:
        linear-gradient(
          90deg,
          rgba(123,63,242,.052),
          rgba(79,111,255,.032),
          rgba(50,195,173,.052)
        );
    }

    .note-card strong {
      display: block;
      margin-bottom: 4px;
      color: var(--navy);
      font-size: 14px;
      line-height: 1.25;
    }

    .note-card span {
      display: block;
      color: var(--muted);
      font-size: 12.5px;
      line-height: 1.35;
    }

    .lists {
      min-height: 0;
      display: grid;
      grid-template-rows: 1fr 1fr;
      gap: 12px;
    }

    .list-card {
      min-height: 0;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      border-radius: 18px;
      border: 1px solid var(--line);
      background: var(--surface-soft);
    }

    .list-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 10px;
      padding: 13px 14px 11px;
      border-bottom: 1px solid var(--line);
      background: rgba(255,255,255,.88);
    }

    .list-header h2 {
      margin: 0;
      color: var(--navy);
      font-size: 17px;
      line-height: 1;
      letter-spacing: -.025em;
      font-weight: 850;
    }

    .count {
      color: var(--muted);
      font-size: 13px;
      font-weight: 800;
      white-space: nowrap;
    }

    .list {
      min-height: 0;
      padding: 9px;
      display: grid;
      gap: 7px;
      overflow: auto;
    }

    .person {
      display: grid;
      grid-template-columns: minmax(0,1fr) auto;
      align-items: center;
      gap: 10px;
      min-height: 48px;
      padding: 8px 11px;
      border-radius: 13px;
      border: 1px solid var(--line);
      background: #FFFFFF;
    }

    .person-main {
      min-width: 0;
    }

    .person strong {
      display: block;
      min-width: 0;
      color: var(--text);
      font-size: 13px;
      line-height: 1.25;
      font-weight: 780;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .person-info {
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 5px;
      margin-top: 4px;
      color: var(--muted-2);
      font-size: 10.5px;
      line-height: 1.2;
    }

    .person-role {
      color: #6442A8;
      font-weight: 750;
    }

    .person-birthday {
      color: #268B78;
      font-weight: 750;
    }

    .person-time {
      color: var(--muted-2);
      font-size: 12px;
      white-space: nowrap;
      font-variant-numeric: tabular-nums;
    }

    .empty {
      padding: 13px;
      border-radius: 13px;
      border: 1px dashed #D4DCEC;
      background: #FFFFFF;
      color: var(--muted);
      font-size: 13px;
      line-height: 1.4;
    }

    details.manage {
      overflow: hidden;
      border-radius: 18px;
      border: 1px solid var(--line);
      background: var(--surface-soft);
    }

    summary {
      list-style: none;
      cursor: pointer;
      padding: 13px 14px;
    }

    summary::-webkit-details-marker {
      display: none;
    }

    .summary-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
    }

    .summary-head h2 {
      margin: 0;
      color: var(--navy);
      font-size: 15px;
      line-height: 1.1;
      letter-spacing: -.02em;
    }

    .summary-head p {
      margin: 4px 0 0;
      color: var(--muted);
      font-size: 12px;
    }

    .chevron {
      width: 28px;
      height: 28px;
      flex: 0 0 auto;
      border-radius: 999px;
      display: grid;
      place-items: center;
      border: 1px solid var(--line);
      background: #F1F4FA;
      color: var(--purple);
      font-weight: 900;
      transition: transform .18s ease;
    }

    details[open] .chevron {
      transform: rotate(180deg);
    }

    .manage-body {
      display: grid;
      gap: 9px;
      padding: 0 14px 14px;
    }

    textarea {
      width: 100%;
      resize: none;
      outline: none;
      border: 1px solid var(--line);
      border-radius: 14px;
      background: #FFFFFF;
      color: var(--text);
      padding: 11px 12px;
      font: inherit;
      font-size: 13px;
      line-height: 1.4;
    }

    textarea:focus {
      border-color: rgba(123,63,242,.42);
      box-shadow: 0 0 0 4px rgba(123,63,242,.08);
    }

    .manage textarea {
      height: min(180px, 25vh);
    }

    .helper {
      margin: 0;
      color: var(--muted);
      font-size: 12px;
      line-height: 1.35;
    }

    .mini-actions {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 9px;
    }

    .modal {
      position: fixed;
      inset: 0;
      z-index: 20;
      display: none;
      align-items: center;
      justify-content: center;
      padding: 24px;
      background: rgba(14,12,48,.42);
      backdrop-filter: blur(4px);
    }

    .modal.visible {
      display: flex;
    }

    .modal-card {
      width: min(860px, calc(100vw - 48px));
      max-height: min(86vh, 760px);
      display: grid;
      grid-template-rows: auto 1fr auto;
      overflow: hidden;
      border-radius: 24px;
      border: 1px solid var(--line);
      background: #FFFFFF;
      box-shadow: 0 28px 80px rgba(17,20,52,.18);
    }

    .modal-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 16px;
      padding: 20px 22px 14px;
      border-bottom: 1px solid var(--line);
    }

    .modal-header h3 {
      margin: 0 0 6px;
      color: var(--navy);
      font-size: 26px;
      line-height: 1;
      letter-spacing: -.045em;
    }

    .modal-header p {
      margin: 0;
      color: var(--muted);
      font-size: 14px;
      line-height: 1.4;
    }

    .close-modal {
      width: 38px;
      height: 38px;
      min-height: 38px;
      padding: 0;
      border-radius: 999px;
      font-size: 22px;
    }

    .modal-body {
      min-height: 0;
      padding: 18px 22px;
      overflow: auto;
      display: grid;
      gap: 14px;
    }

    .summary-highlights {
      display: grid;
      grid-template-columns: repeat(4, minmax(0,1fr));
      gap: 10px;
    }

    .highlight {
      padding: 12px 13px;
      border-radius: 15px;
      border: 1px solid var(--line);
      background: var(--surface-soft);
    }

    .highlight span {
      display: block;
      margin-bottom: 7px;
      color: var(--muted);
      font-size: 10.5px;
      font-weight: 850;
      letter-spacing: .09em;
      text-transform: uppercase;
    }

    .highlight strong {
      color: var(--navy);
      font-size: 22px;
      line-height: 1;
      font-weight: 900;
      font-variant-numeric: tabular-nums;
    }

    .summary-text {
      min-height: 260px;
      max-height: 42vh;
    }

    .modal-actions {
      display: flex;
      justify-content: flex-end;
      gap: 10px;
      padding: 15px 22px 20px;
      border-top: 1px solid var(--line);
    }

    .toast {
      position: fixed;
      left: 50%;
      bottom: 22px;
      z-index: 30;
      transform: translateX(-50%) translateY(14px);
      opacity: 0;
      pointer-events: none;
      padding: 11px 15px;
      border-radius: 999px;
      background: rgba(16,10,102,.96);
      color: #FFFFFF;
      font-size: 13px;
      font-weight: 800;
      transition: all .2s ease;
    }

    .toast.visible {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }

    @media (max-width: 1180px) {
      .layout {
        grid-template-columns: minmax(0,1fr) 360px;
      }

      .metrics {
        grid-template-columns: repeat(2, minmax(0,1fr));
      }
    }

    @media (max-width: 900px) {
      html,
      body {
        overflow: auto;
      }

      .app {
        height: auto;
        min-height: 100vh;
        width: calc(100vw - 18px);
        padding: 12px 0;
      }

      .topbar,
      .layout {
        grid-template-columns: 1fr;
      }

      .status-pill {
        justify-self: start;
      }

      .side-inner {
        min-height: 640px;
      }
    }

    @media (max-height: 760px) and (min-width: 901px) {
      .app {
        padding: 10px 0;
        gap: 10px;
      }

      .topbar {
        min-height: 62px;
      }

      h1 {
        font-size: clamp(24px, 2.4vw, 32px);
      }

      .subtitle {
        margin-top: 5px;
        font-size: 13px;
      }

      .panel-inner,
      .side-inner {
        padding: 14px;
        gap: 10px;
      }

      .metric {
        min-height: 64px;
        padding: 10px 12px;
      }

      .speaker-card {
        min-height: 190px;
        padding: 18px;
      }

      .speaker-name {
        font-size: clamp(30px, 3.4vw, 50px);
      }

      .speaker-meta {
        margin-top: 12px;
      }

      .speaker-badge {
        min-height: 29px;
        padding: 6px 10px;
        font-size: 11px;
      }

      button {
        min-height: 42px;
        padding: 10px 12px;
        font-size: 13px;
      }

      .person {
        min-height: 42px;
        padding: 6px 9px;
      }
    }
  </style>
</head>

<body>

  <div class="app">

    <header class="topbar">

      <div>
        <div class="eyebrow">Frenet · Daily Expansão</div>

        <div class="title-row">
          <h1>Sorteador da Daily</h1>
          <span class="team-chip">Expansão</span>
        </div>

        <p class="subtitle">
          Organize a ordem de fala da Daily de Expansão, registre os tempos
          automaticamente e gere um resumo final ao encerrar a call.
        </p>
      </div>

      <div class="status-pill">
        <span class="dot" id="statusDot"></span>
        <span id="statusText">Não iniciado</span>
      </div>

    </header>

    <main class="layout">

      <section class="panel main-panel">

        <div class="panel-inner">

          <div class="metrics">

            <div class="metric">
              <div class="metric-label">Participantes</div>
              <div class="metric-value" id="participantTotal">13</div>
            </div>

            <div class="metric">
              <div class="metric-label">Falas concluídas</div>
              <div class="metric-value" id="spokenMetric">0</div>
            </div>

            <div class="metric">
              <div class="metric-label">Tempo restante</div>
              <div class="metric-value" id="speakerLimitMetric">02:18</div>
            </div>

            <div class="metric">
              <div class="metric-label">Tempo da call</div>
              <div class="metric-value" id="meetingTimer">00:00</div>
            </div>

          </div>

          <div class="progress-card">

            <span>Andamento</span>

            <div class="progress-wrap">
              <div class="progress-bar" id="progressBar"></div>
            </div>

            <span id="progressLabel">0 de 13 falas</span>

          </div>

          <div class="speaker-card" id="speakerCard">

            <div class="speaker-eyebrow" id="speakerEyebrow">
              Falando agora
            </div>

            <h2 class="speaker-name" id="speakerName">
              Pronto para iniciar
            </h2>

            <div class="speaker-meta hidden" id="speakerMeta">

              <span
                class="speaker-badge role hidden"
                id="speakerRole"
              ></span>

              <span
                class="speaker-badge birthday hidden"
                id="speakerBirthday"
              ></span>

            </div>

            <div class="speaker-email" id="speakerEmail">
              Clique em iniciar para sortear a primeira pessoa.
            </div>

            <div class="speaker-timer" id="speakerTimerWrap">

              restante
              <b id="speakerRemaining">02:18</b>

              · usado
              <b id="speakerTimer">00:00</b>

              · limite
              <b id="speakerLimitInline">02:18</b>

            </div>

          </div>

          <div class="actions">

            <button class="primary" id="startBtn">
              Iniciar sorteio
            </button>

            <button class="success" id="finishSpeakerBtn" disabled>
              Concluir fala
            </button>

            <button class="warning" id="pauseBtn" disabled>
              Pausar
            </button>

            <button class="danger" id="endBtn" disabled>
              Encerrar call
            </button>

          </div>

        </div>

      </section>

      <aside class="panel">

        <div class="side-inner">

          <div class="note-card">

            <strong>Tempo de fala calculado automaticamente</strong>

            <span>
              O restante por pessoa começa a contar assim que a fala é
              sorteada. O cálculo considera 30 minutos divididos pelo total
              de participantes.
            </span>

          </div>

          <div class="lists">

            <section class="list-card">

              <div class="list-header">
                <h2>Faltam falar</h2>
                <span class="count" id="remainingCount">13</span>
              </div>

              <div class="list" id="remainingList"></div>

            </section>

            <section class="list-card">

              <div class="list-header">
                <h2>Já falaram</h2>
                <span class="count" id="spokenCount">0</span>
              </div>

              <div class="list" id="spokenList"></div>

            </section>

          </div>

          <details class="manage">

            <summary>

              <div class="summary-head">

                <div>
                  <h2>Gerenciar participantes</h2>
                  <p>Ajuste a lista antes de começar.</p>
                </div>

                <span class="chevron">⌄</span>

              </div>

            </summary>

            <div class="manage-body">

              <textarea id="participantsInput"></textarea>

              <p class="helper">
                Formato: Nome &lt;e-mail&gt; | Cargo | Aniversário (DD/MM). Cargo e aniversário podem ficar em branco.
              </p>

              <div class="mini-actions">

                <button class="secondary" id="applyParticipantsBtn">
                  Aplicar lista
                </button>

                <button class="secondary" id="resetBtn">
                  Reiniciar tudo
                </button>

              </div>

            </div>

          </details>

        </div>

      </aside>

    </main>

  </div>


  <div class="modal" id="summaryModal" aria-hidden="true">

    <div class="modal-card">

      <div class="modal-header">

        <div>
          <h3>Resumo final da Daily de Expansão</h3>
          <p>Fechamento automático da call.</p>
        </div>

        <button
          class="close-modal secondary"
          id="closeSummaryBtn"
        >
          ×
        </button>

      </div>

      <div class="modal-body">

        <div class="summary-highlights">

          <div class="highlight">
            <span>Duração total</span>
            <strong id="summaryDuration">00:00</strong>
          </div>

          <div class="highlight">
            <span>Falas registradas</span>
            <strong id="summarySpoken">0</strong>
          </div>

          <div class="highlight">
            <span>Pendentes</span>
            <strong id="summaryPending">0</strong>
          </div>

          <div class="highlight">
            <span>Média por fala</span>
            <strong id="summaryAverage">00:00</strong>
          </div>

        </div>

        <textarea
          class="summary-text"
          id="summaryText"
          readonly
        ></textarea>

      </div>

      <div class="modal-actions">

        <button class="primary" id="restartSummaryBtn">
          Reiniciar do zero
        </button>

        <button class="secondary" id="copySummaryBtn">
          Copiar resumo
        </button>

        <button class="secondary" id="downloadSummaryBtn">
          Baixar .txt
        </button>

      </div>

    </div>

  </div>

  <div class="toast" id="toast"></div>


  <script>

    const defaultParticipantsText = `Alef David <alef.nascimento@frenet.com.br> | |
Alexandre Spreafico Novaes <alexandre.novaes@frenet.com.br> | |
André Luis da Costa <andre.costa@frenet.com.br> | |
Andréia Medeiros <andreia.medeiros@frenet.com.br> | |
Arthur Nascimento <arthur.nascimento@frenet.com.br> | |
Beatriz Vendrame <beatriz@frenet.com.br> | |
Diego Massatoshi Miyabara <diego.miyabara@frenet.com.br> | |
Elias Fernandes Cotrim <elias.cotrim@frenet.com.br> | |
Giovanna Cinoca <giovanna.paula@frenet.com.br> | |
Guilherme Henrique S. Lima <guilherme.lima@frenet.com.br> | |
Ricardo Delgado Filho <ricardo.filho@frenet.com.br> | |
Thallysson Makcyel <thallysson.barbosa@frenet.com.br> | |
Vitor Camilo <vitor.camilo@frenet.com.br> | |`;


    const MEETING_DURATION_SECONDS = 30 * 60;


    const state = {
      started: false,
      paused: false,
      ended: false,

      remaining: [],
      spoken: [],
      current: null,

      drawing: false,
      drawingNamePreview: "",
      drawingIntervalId: null,
      revealTimeoutId: null,

      elapsedSeconds: 0,
      currentSpeakerSeconds: 0,

      intervalId: null,

      startedAt: null,
      endedAt: null
    };


    const els = {

      statusDot:
        document.getElementById("statusDot"),

      statusText:
        document.getElementById("statusText"),

      meetingTimer:
        document.getElementById("meetingTimer"),

      speakerTimer:
        document.getElementById("speakerTimer"),

      speakerCard:
        document.getElementById("speakerCard"),

      speakerEyebrow:
        document.getElementById("speakerEyebrow"),

      speakerName:
        document.getElementById("speakerName"),

      speakerMeta:
        document.getElementById("speakerMeta"),

      speakerRole:
        document.getElementById("speakerRole"),

      speakerBirthday:
        document.getElementById("speakerBirthday"),

      speakerEmail:
        document.getElementById("speakerEmail"),

      participantTotal:
        document.getElementById("participantTotal"),

      spokenMetric:
        document.getElementById("spokenMetric"),

      speakerLimitMetric:
        document.getElementById("speakerLimitMetric"),

      speakerTimerWrap:
        document.getElementById("speakerTimerWrap"),

      speakerLimitInline:
        document.getElementById("speakerLimitInline"),

      speakerRemaining:
        document.getElementById("speakerRemaining"),

      progressLabel:
        document.getElementById("progressLabel"),

      progressBar:
        document.getElementById("progressBar"),

      remainingCount:
        document.getElementById("remainingCount"),

      spokenCount:
        document.getElementById("spokenCount"),

      remainingList:
        document.getElementById("remainingList"),

      spokenList:
        document.getElementById("spokenList"),

      startBtn:
        document.getElementById("startBtn"),

      finishSpeakerBtn:
        document.getElementById("finishSpeakerBtn"),

      pauseBtn:
        document.getElementById("pauseBtn"),

      endBtn:
        document.getElementById("endBtn"),

      resetBtn:
        document.getElementById("resetBtn"),

      participantsInput:
        document.getElementById("participantsInput"),

      applyParticipantsBtn:
        document.getElementById("applyParticipantsBtn"),

      summaryModal:
        document.getElementById("summaryModal"),

      closeSummaryBtn:
        document.getElementById("closeSummaryBtn"),

      summaryText:
        document.getElementById("summaryText"),

      summaryDuration:
        document.getElementById("summaryDuration"),

      summarySpoken:
        document.getElementById("summarySpoken"),

      summaryPending:
        document.getElementById("summaryPending"),

      summaryAverage:
        document.getElementById("summaryAverage"),

      restartSummaryBtn:
        document.getElementById("restartSummaryBtn"),

      copySummaryBtn:
        document.getElementById("copySummaryBtn"),

      downloadSummaryBtn:
        document.getElementById("downloadSummaryBtn"),

      toast:
        document.getElementById("toast")
    };


    function formatTime(totalSeconds) {

      const safeSeconds =
        Math.max(
          0,
          Math.floor(totalSeconds)
        );

      const hours =
        Math.floor(
          safeSeconds / 3600
        );

      const minutes =
        Math.floor(
          (safeSeconds % 3600) / 60
        )
          .toString()
          .padStart(2, "0");

      const seconds =
        (safeSeconds % 60)
          .toString()
          .padStart(2, "0");

      return hours > 0
        ? `${hours}:${minutes}:${seconds}`
        : `${minutes}:${seconds}`;

    }


    function getTotalParticipants() {

      return (
        state.remaining.length +
        state.spoken.length +
        (state.current ? 1 : 0)
      );

    }


    function getSpeakerLimitSeconds() {

      const total =
        getTotalParticipants();

      return total > 0
        ? Math.floor(
            MEETING_DURATION_SECONDS / total
          )
        : 0;

    }


    function parseParticipants(text) {

      const lines =
        text
          .split(/\n/)
          .map(line => line.trim())
          .filter(Boolean);


      const seen =
        new Set();


      return lines
        .map((line, index) => {

          const parts =
            line
              .split("|")
              .map(part => part.trim());


          const identityPart =
            parts[0] || "";

          const role =
            parts[1] || "";

          const birthday =
            parts[2] || "";


          const match =
            identityPart.match(
              /^(.*?)\s*<([^>]+)>$/
            );


          let name = "";
          let email = "";


          if (match) {

            name =
              match[1].trim();

            email =
              match[2].trim();

          }

          else if (
            identityPart.includes("@")
          ) {

            email =
              identityPart
                .replace(/[<>]/g, "")
                .trim();

            name =
              email
                .split("@")[0]
                .replace(/[._-]+/g, " ");

            name =
              name.replace(
                /\b\w/g,
                char => char.toUpperCase()
              );

          }

          else {

            name =
              identityPart.trim();

          }


          if (!name) {
            name =
              `Participante ${index + 1}`;
          }


          const key =
            (email || name)
              .toLowerCase();


          if (seen.has(key)) {
            return null;
          }


          seen.add(key);


          return {

            id:
              crypto.randomUUID
                ? crypto.randomUUID()
                : `${Date.now()}-${index}-${Math.random()}`,

            name,
            email,
            role,
            birthday

          };

        })
        .filter(Boolean);

    }


    function escapeHtml(value) {

      return String(value)
        .replaceAll("&", "&amp;")
        .replaceAll("<", "&lt;")
        .replaceAll(">", "&gt;")
        .replaceAll('"', "&quot;")
        .replaceAll("'", "&#039;");

    }


    function toast(message) {

      els.toast.textContent =
        message;

      els.toast.classList.add(
        "visible"
      );

      setTimeout(
        () => {
          els.toast.classList.remove(
            "visible"
          );
        },
        2300
      );

    }


    function updateStatus() {

      if (state.ended) {

        els.statusText.textContent =
          "Encerrado";

        els.statusDot.style.background =
          "var(--danger)";

        els.statusDot.style.boxShadow =
          "0 0 0 5px rgba(217,79,112,.15)";

        return;

      }


      if (!state.started) {

        els.statusText.textContent =
          "Não iniciado";

        els.statusDot.style.background =
          "var(--warning)";

        els.statusDot.style.boxShadow =
          "0 0 0 5px rgba(244,178,62,.15)";

        return;

      }


      if (state.paused) {

        els.statusText.textContent =
          "Pausado";

        els.statusDot.style.background =
          "var(--warning)";

        return;

      }


      els.statusText.textContent =
        "Em andamento";

      els.statusDot.style.background =
        "var(--success)";

      els.statusDot.style.boxShadow =
        "0 0 0 5px rgba(25,167,125,.15)";

    }


    function updateButtons() {

      if (state.drawing) {

        els.startBtn.disabled =
          true;

        els.startBtn.textContent =
          "Sorteando...";

      }

      else if (state.ended) {

        els.startBtn.disabled =
          false;

        els.startBtn.textContent =
          "Reiniciar do zero";

      }

      else {

        els.startBtn.disabled =
          (
            state.started &&
            !!state.current
          ) ||
          state.remaining.length === 0;

        els.startBtn.textContent =
          state.started
            ? "Sortear próximo"
            : "Iniciar sorteio";

      }


      els.finishSpeakerBtn.disabled =
        !state.current ||
        state.ended ||
        state.drawing;


      els.pauseBtn.disabled =
        !state.started ||
        state.ended ||
        state.drawing;


      els.pauseBtn.textContent =
        state.paused
          ? "Retomar"
          : "Pausar";


      els.endBtn.disabled =
        !state.started ||
        state.ended ||
        state.drawing;


      els.applyParticipantsBtn.disabled =
        state.started &&
        !state.ended;


      els.participantsInput.disabled =
        state.started &&
        !state.ended;

    }


    function updateMetrics() {

      const total =
        getTotalParticipants();

      const completed =
        state.spoken.length;

      const progress =
        total > 0
          ? (completed / total) * 100
          : 0;

      const speakerLimit =
        getSpeakerLimitSeconds();

      const speakerRemaining =
        speakerLimit -
        state.currentSpeakerSeconds;


      els.participantTotal.textContent =
        total;

      els.spokenMetric.textContent =
        completed;


      const remainingLabel =
        speakerRemaining >= 0

          ? formatTime(
              speakerRemaining
            )

          : `-${formatTime(
              Math.abs(
                speakerRemaining
              )
            )}`;


      els.speakerLimitMetric.textContent =
        state.current
          ? remainingLabel
          : formatTime(
              speakerLimit
            );


      els.meetingTimer.textContent =
        formatTime(
          state.elapsedSeconds
        );


      els.speakerTimer.textContent =
        formatTime(
          state.currentSpeakerSeconds
        );


      els.speakerLimitInline.textContent =
        formatTime(
          speakerLimit
        );


      els.speakerRemaining.textContent =
        state.current
          ? remainingLabel
          : formatTime(
              speakerLimit
            );


      els.progressLabel.textContent =
        `${completed} de ${total} falas`;


      els.progressBar.style.width =
        `${Math.max(
          0,
          Math.min(
            100,
            progress
          )
        )}%`;


      els.speakerTimerWrap.classList.remove(
        "warning",
        "over"
      );


      if (
        state.current &&
        speakerLimit > 0
      ) {

        if (
          state.currentSpeakerSeconds >
          speakerLimit
        ) {

          els.speakerTimerWrap.classList.add(
            "over"
          );

        }

        else if (
          state.currentSpeakerSeconds >=
          Math.floor(
            speakerLimit * .8
          )
        ) {

          els.speakerTimerWrap.classList.add(
            "warning"
          );

        }

      }

    }


    function personHtml(
      person,
      extra = ""
    ) {

      const roleHtml =
        person.role
          ? `<span class="person-role">${escapeHtml(person.role)}</span>`
          : "";


      const separator =
        person.role &&
        person.birthday
          ? `<span>·</span>`
          : "";


      const birthdayHtml =
        person.birthday
          ? `<span class="person-birthday">Aniv. ${escapeHtml(person.birthday)}</span>`
          : "";


      const right =
        extra
          ? `<span class="person-time">${escapeHtml(extra)}</span>`
          : "";


      return `
        <div class="person">

          <div class="person-main">

            <strong
              title="${escapeHtml(person.name)}"
            >
              ${escapeHtml(person.name)}
            </strong>

            <div class="person-info">
              ${roleHtml}
              ${separator}
              ${birthdayHtml}
            </div>

          </div>

          ${right}

        </div>
      `;

    }


    function renderLists() {

      els.remainingCount.textContent =
        state.remaining.length;


      els.spokenCount.textContent =
        state.spoken.length;


      els.remainingList.innerHTML =
        state.remaining.length

          ? state.remaining
              .map(
                person =>
                  personHtml(person)
              )
              .join("")

          : `<div class="empty">
              Ninguém pendente.
            </div>`;


      els.spokenList.innerHTML =
        state.spoken.length

          ? state.spoken
              .map(
                item =>
                  personHtml(
                    item,
                    formatTime(
                      item.seconds
                    )
                  )
              )
              .join("")

          : `<div class="empty">
              Nenhuma fala concluída ainda.
            </div>`;

    }


    function hideSpeakerMeta() {

      els.speakerMeta.classList.add(
        "hidden"
      );

      els.speakerRole.classList.add(
        "hidden"
      );

      els.speakerBirthday.classList.add(
        "hidden"
      );

    }


    function renderCurrentSpeaker() {

      els.speakerCard.classList.toggle(
        "drawing",
        state.drawing
      );


      if (state.drawing) {

        hideSpeakerMeta();

        els.speakerEyebrow.textContent =
          "Sorteando...";

        els.speakerName.textContent =
          state.drawingNamePreview ||
          "Sorteando";

        els.speakerEmail.textContent =
          "Aguarde a roleta definir a próxima pessoa.";

        return;

      }


      els.speakerEyebrow.textContent =
        "Falando agora";


      if (state.current) {

        els.speakerName.textContent =
          state.current.name;


        /*
          Mostra o bloco de metadados somente
          se existir cargo ou aniversário.
        */
        const hasMeta =
          !!state.current.role ||
          !!state.current.birthday;


        els.speakerMeta.classList.toggle(
          "hidden",
          !hasMeta
        );


        if (state.current.role) {

          els.speakerRole.textContent =
            state.current.role;

          els.speakerRole.classList.remove(
            "hidden"
          );

        }

        else {

          els.speakerRole.classList.add(
            "hidden"
          );

        }


        if (state.current.birthday) {

          els.speakerBirthday.textContent =
            `Aniversário · ${state.current.birthday}`;

          els.speakerBirthday.classList.remove(
            "hidden"
          );

        }

        else {

          els.speakerBirthday.classList.add(
            "hidden"
          );

        }


        els.speakerEmail.textContent =
          state.current.email || "";

        return;

      }


      hideSpeakerMeta();


      if (state.ended) {

        els.speakerName.textContent =
          "Call encerrada";

        els.speakerEmail.textContent =
          "O resumo final foi preparado.";

        return;

      }


      if (
        state.started &&
        state.remaining.length === 0
      ) {

        els.speakerName.textContent =
          "Todos falaram";

        els.speakerEmail.textContent =
          "Você já pode encerrar a call.";

        return;

      }


      if (state.started) {

        els.speakerName.textContent =
          "Aguardando sorteio";

        els.speakerEmail.textContent =
          "Clique em sortear próximo.";

        return;

      }


      els.speakerName.textContent =
        "Pronto para iniciar";

      els.speakerEmail.textContent =
        "Clique em iniciar para sortear a primeira pessoa.";

    }


    function render() {

      updateStatus();
      updateButtons();
      updateMetrics();
      renderLists();
      renderCurrentSpeaker();

    }


    function startClock() {

      if (state.intervalId) {
        return;
      }


      state.intervalId =
        setInterval(() => {

          if (
            !state.started ||
            state.paused ||
            state.ended
          ) {
            return;
          }


          state.elapsedSeconds += 1;


          if (state.current) {

            state.currentSpeakerSeconds += 1;


            const speakerLimit =
              getSpeakerLimitSeconds();


            if (
              speakerLimit > 0 &&
              state.currentSpeakerSeconds ===
              speakerLimit
            ) {

              toast(
                "Tempo sugerido de fala atingido."
              );

            }

          }


          render();

        }, 1000);

    }


    function drawNextSpeaker() {

      if (state.drawing) {
        return;
      }


      if (
        state.remaining.length === 0
      ) {

        toast(
          "Todos os participantes já falaram."
        );

        return;

      }


      const candidates =
        [...state.remaining];


      let previewIndex = 0;


      state.drawing =
        true;


      state.drawingNamePreview =
        candidates[0]?.name ||
        "Sorteando";


      state.currentSpeakerSeconds =
        0;


      render();


      state.drawingIntervalId =
        setInterval(() => {

          previewIndex =
            (previewIndex + 1) %
            candidates.length;


          state.drawingNamePreview =
            candidates[
              previewIndex
            ].name;


          renderCurrentSpeaker();

        }, 90);


      state.revealTimeoutId =
        setTimeout(() => {

          clearInterval(
            state.drawingIntervalId
          );


          state.drawingIntervalId =
            null;


          state.revealTimeoutId =
            null;


          const randomIndex =
            Math.floor(
              Math.random() *
              state.remaining.length
            );


          state.current =
            state.remaining.splice(
              randomIndex,
              1
            )[0];


          state.currentSpeakerSeconds =
            0;


          state.drawing =
            false;


          state.drawingNamePreview =
            "";


          render();

        }, 1700);

    }


    function startOrDraw() {

      if (state.drawing) {
        return;
      }


      if (state.ended) {

        resetAll();

        toast(
          "Sorteio reiniciado do zero."
        );

        return;

      }


      if (!state.started) {

        state.started =
          true;

        state.paused =
          false;

        state.ended =
          false;

        state.startedAt =
          new Date();

        startClock();

      }


      if (!state.current) {
        drawNextSpeaker();
      }

    }


    function finishCurrentSpeaker() {

      if (!state.current) {
        return;
      }


      state.spoken.push({

        ...state.current,

        seconds:
          state.currentSpeakerSeconds

      });


      state.current =
        null;


      state.currentSpeakerSeconds =
        0;


      if (
        state.remaining.length === 0
      ) {

        toast(
          "Todos falaram. Você já pode encerrar a call."
        );

      }


      render();

    }


    function togglePause() {

      if (
        !state.started ||
        state.ended
      ) {
        return;
      }


      state.paused =
        !state.paused;


      render();

    }


    function openSummaryModal() {

      els.summaryModal.classList.add(
        "visible"
      );

      els.summaryModal.setAttribute(
        "aria-hidden",
        "false"
      );

    }


    function closeSummaryModal() {

      els.summaryModal.classList.remove(
        "visible"
      );

      els.summaryModal.setAttribute(
        "aria-hidden",
        "true"
      );

    }


    function endDaily() {

      if (state.drawing) {
        return;
      }


      if (state.current) {

        const shouldRegister =
          confirm(
            "Existe uma pessoa falando agora. Deseja registrar essa fala com o tempo atual antes de encerrar?"
          );


        if (shouldRegister) {
          finishCurrentSpeaker();
        }

      }


      state.ended =
        true;


      state.paused =
        false;


      state.endedAt =
        new Date();


      if (state.intervalId) {

        clearInterval(
          state.intervalId
        );

        state.intervalId =
          null;

      }


      generateSummary();

      render();

      openSummaryModal();

    }


    function generateSummary() {

      const speakerLimit =
        getSpeakerLimitSeconds();


      const totalUsed =
        state.spoken.reduce(
          (sum, item) =>
            sum + item.seconds,
          0
        );


      const avg =
        state.spoken.length
          ? totalUsed /
            state.spoken.length
          : 0;


      const pendingParticipants =
        state.current
          ? [
              ...state.remaining,
              state.current
            ]
          : [
              ...state.remaining
            ];


      const missingNames =
        pendingParticipants
          .map(
            item =>
              `- ${item.name}`
          )
          .join("\n") ||
        "Nenhum";


      const spokenLines =
        state.spoken
          .map(
            (item, index) => {

              const roleLabel =
                item.role
                  ? ` · ${item.role}`
                  : "";


              const exceededSeconds =
                item.seconds -
                speakerLimit;


              const exceededLabel =
                exceededSeconds > 0
                  ? ` · excedeu ${formatTime(exceededSeconds)}`
                  : "";


              return (
                `${index + 1}. ` +
                `${item.name}` +
                `${roleLabel}` +
                ` — ${formatTime(item.seconds)}` +
                `${exceededLabel}`
              );

            }
          )
          .join("\n") ||
        "Nenhuma fala registrada.";


      const exceededLines =
        state.spoken
          .filter(
            item =>
              item.seconds >
              speakerLimit
          )
          .map(
            item =>
              `- ${item.name} — ` +
              `${formatTime(item.seconds)} de fala · excedeu ` +
              `${formatTime(
                item.seconds -
                speakerLimit
              )}`
          )
          .join("\n") ||
        "Nenhum participante excedeu o tempo sugerido.";


      const startedAt =
        state.startedAt
          ? state.startedAt.toLocaleString(
              "pt-BR"
            )
          : "-";


      const endedAt =
        state.endedAt
          ? state.endedAt.toLocaleString(
              "pt-BR"
            )
          : "-";


      const summary = `Resumo da Daily de Expansão — Frenet

Início: ${startedAt}
Fim: ${endedAt}
Duração total da call: ${formatTime(state.elapsedSeconds)}
Participantes na lista: ${getTotalParticipants()}
Participantes que falaram: ${state.spoken.length}
Participantes pendentes: ${pendingParticipants.length}
Tempo registrado em falas: ${formatTime(totalUsed)}
Tempo médio por fala: ${formatTime(avg)}
Limite por fala considerado: ${formatTime(speakerLimit)}

Tempo de fala por participante:
${spokenLines}

Quem excedeu o tempo sugerido:
${exceededLines}

Quem ficou pendente:
${missingNames}`;


      els.summaryDuration.textContent =
        formatTime(
          state.elapsedSeconds
        );


      els.summarySpoken.textContent =
        state.spoken.length;


      els.summaryPending.textContent =
        pendingParticipants.length;


      els.summaryAverage.textContent =
        formatTime(avg);


      els.summaryText.value =
        summary;

    }


    async function copySummary() {

      try {

        await navigator.clipboard.writeText(
          els.summaryText.value
        );

        toast(
          "Resumo copiado."
        );

      }

      catch {

        els.summaryText.select();

        document.execCommand(
          "copy"
        );

        toast(
          "Resumo copiado."
        );

      }

    }


    function downloadSummary() {

      const blob =
        new Blob(
          [els.summaryText.value],
          {
            type:
              "text/plain;charset=utf-8"
          }
        );


      const url =
        URL.createObjectURL(
          blob
        );


      const link =
        document.createElement(
          "a"
        );


      link.href =
        url;


      link.download =
        "resumo-daily-expansao-frenet.txt";


      link.click();


      URL.revokeObjectURL(
        url
      );

    }


    function resetAll() {

      if (state.intervalId) {

        clearInterval(
          state.intervalId
        );

        state.intervalId =
          null;

      }


      if (
        state.drawingIntervalId
      ) {

        clearInterval(
          state.drawingIntervalId
        );

        state.drawingIntervalId =
          null;

      }


      if (
        state.revealTimeoutId
      ) {

        clearTimeout(
          state.revealTimeoutId
        );

        state.revealTimeoutId =
          null;

      }


      const participants =
        parseParticipants(
          els.participantsInput.value
        );


      state.started =
        false;

      state.paused =
        false;

      state.ended =
        false;

      state.remaining =
        participants;

      state.spoken =
        [];

      state.current =
        null;

      state.drawing =
        false;

      state.drawingNamePreview =
        "";

      state.elapsedSeconds =
        0;

      state.currentSpeakerSeconds =
        0;

      state.startedAt =
        null;

      state.endedAt =
        null;


      closeSummaryModal();


      els.summaryText.value =
        "";

      els.summaryDuration.textContent =
        "00:00";

      els.summarySpoken.textContent =
        "0";

      els.summaryPending.textContent =
        "0";

      els.summaryAverage.textContent =
        "00:00";


      render();

    }


    function applyParticipants() {

      const participants =
        parseParticipants(
          els.participantsInput.value
        );


      if (
        participants.length === 0
      ) {

        toast(
          "Inclua ao menos um participante."
        );

        return;

      }


      resetAll();


      toast(
        "Lista aplicada e sorteio reiniciado."
      );

    }


    els.startBtn.addEventListener(
      "click",
      startOrDraw
    );


    els.finishSpeakerBtn.addEventListener(
      "click",
      finishCurrentSpeaker
    );


    els.pauseBtn.addEventListener(
      "click",
      togglePause
    );


    els.endBtn.addEventListener(
      "click",
      endDaily
    );


    els.resetBtn.addEventListener(
      "click",
      resetAll
    );


    els.applyParticipantsBtn.addEventListener(
      "click",
      applyParticipants
    );


    els.restartSummaryBtn.addEventListener(
      "click",
      () => {
        resetAll();
        toast(
          "Sorteio reiniciado do zero."
        );
      }
    );


    els.copySummaryBtn.addEventListener(
      "click",
      copySummary
    );


    els.downloadSummaryBtn.addEventListener(
      "click",
      downloadSummary
    );


    els.closeSummaryBtn.addEventListener(
      "click",
      closeSummaryModal
    );


    els.summaryModal.addEventListener(
      "click",
      event => {

        if (
          event.target ===
          els.summaryModal
        ) {
          closeSummaryModal();
        }

      }
    );


    document.addEventListener(
      "keydown",
      event => {

        if (
          event.key ===
          "Escape"
        ) {
          closeSummaryModal();
        }

      }
    );


    els.participantsInput.value =
      defaultParticipantsText;


    resetAll();

  </script>

</body>
</html>
